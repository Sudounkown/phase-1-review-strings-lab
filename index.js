// Write your code in this file!
let currentUser = "Someone"
let welcomeMessage = `${currentUser} "Welcome to Flatbook, !`
const upperCaseWelcomMessage = currentUser.toUpperCase()
let excitedWelcomeMessage = ` ${upperCaseWelcomMessage} WELCOME TO FLATBOOK, !`
const firstInitial = currentUser[0]
const restOfName = currentUser.slice(1)

let shortGreeting = `Welcome,  ${firstInitial} !`